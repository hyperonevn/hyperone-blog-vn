backend:
  name: git-gateway
  branch: main

media_folder: "public/images/uploads"    # nơi lưu ảnh trong repo
public_folder: "/images/uploads"          # đường dẫn public để hiển thị ảnh

collections:
  - name: "blog"
    label: "Blog"
    folder: "data/blog"                   # trỏ đúng thư mục chứa bài viết
    create: true
    slug: "{{slug}}"
    extension: "mdx"                      # đúng định dạng bạn đang dùng
    format: "frontmatter"
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Date", name: "date", widget: "datetime" }
      - { label: "Description", name: "description", widget: "string", required: false }
      - { label: "Body", name: "body", widget: "markdown" }
